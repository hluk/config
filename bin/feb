#!/bin/bash
# browse images given as parameter or images in current directory
test $# -eq 0 && ARGS=. || ARGS=$@
# quotes hell!
feh -F -S filename "$ARGS" --action1 '
	PIC_ROOT=~/Pictures
	DEST=$(~/dev/menus/imgmenu.sh) || exit 1
	test -n "$DEST" && mv "%f" "$PIC_ROOT/$DEST/"'

